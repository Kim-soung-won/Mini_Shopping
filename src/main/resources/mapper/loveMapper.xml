<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="userSQL">
    <insert id="insertLove">
        INSERT INTO love (product_id, created_who, created_at)
        VALUES (#{productId}, #{createdWho}, NOW())
    </insert>

    <delete id="deleteLove">
        DELETE FROM love
        WHERE created_who = #{userId} AND product_id = #{productId}
    </delete>

    <select id="selectLovesByUserId" resultType="LoveDTO">
        SELECT * FROM love
        WHERE created_who = #{userId}
    </select>

    <select id="findLoveByUserIdAndProductId" resultType="LoveDTO">
        SELECT * FROM love
        WHERE created_who = #{userId} AND product_id = #{productId}
        LIMIT 1
    </select>
</mapper>